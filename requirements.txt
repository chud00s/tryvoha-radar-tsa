# Core data
pandas>=2.0,<3.0
numpy>=1.24,<2.1
pyarrow>=12.0          # parquet engine

# Modeling (upper bounds pinned so committed model_metrics.json reproduces)
scikit-learn>=1.3,<1.7

# Viz / UI
streamlit>=1.30,<2.0
plotly>=5.24          # go.Choroplethmap / px.scatter_map (MapLibre)
folium>=0.16          # OSINT live map (clickable markers)
streamlit-folium>=0.20

# IO / config
requests>=2.31
python-dotenv>=1.0

# AI extraction (optional at runtime — pipeline has a no-key fallback)
anthropic>=0.40,<1.0

# Telegram OSINT (stretch / live mode)
telethon>=1.34

# Tests
pytest>=7.4
